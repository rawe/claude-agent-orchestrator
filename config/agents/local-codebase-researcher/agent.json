{
  "name": "local-codebase-researcher",
  "description": "Researches a local codebase to answer questions. Performs iterative search and analysis, produces concise findings with documented file references.",
  "type": "autonomous",
  "tags": ["internal", "research", "code"],
  "parameters_schema": {
    "type": "object",
    "required": ["question", "project_dir"],
    "properties": {
      "question": {
        "type": "string",
        "minLength": 10,
        "description": "The research question. Be specific - include function names, class names, or relevant context."
      },
      "project_dir": {
        "type": "string",
        "description": "Absolute path to the codebase to search."
      }
    },
    "additionalProperties": false
  }
}
