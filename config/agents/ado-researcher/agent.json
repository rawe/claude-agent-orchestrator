{
  "name": "ado-researcher",
  "description": "Researches Azure DevOps to answer questions. Performs iterative search and analysis, produces concise answer with documented sources.",
  "type": "autonomous",
  "tags": ["internal", "research", "ado"],
  "capabilities": ["ado"],
  "parameters_schema": {
    "type": "object",
    "required": ["question"],
    "properties": {
      "question": {
        "type": "string",
        "minLength": 10,
        "description": "The research question. Be specific - include project names, work item types, or relevant context."
      },
      "working_folder": {
        "type": "string",
        "description": "Folder path for results. Auto-generated as research-results/[date]-[topic] if omitted."
      }
    },
    "additionalProperties": false
  }
}
