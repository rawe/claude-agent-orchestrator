{
  "name": "json_checker",
  "description": "Checks a given file for proper JSON format",
  "script_file": "json_check.py",
  "parameters_schema": {
    "type": "object",
    "properties": {
      "file": {
        "type": "string",
        "description": "Path to the file to validate as JSON"
      }
    },
    "required": [
      "file"
    ]
  },
  "demands": {
    "tags": [
      "uv"
    ]
  }
}
