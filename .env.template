# Agent Orchestrator Environment Configuration
# Copy this file to .env and customize as needed:
#   cp .env.template .env

# ==============================================================================
# AUTHENTICATION
# See docs/architecture/auth-coordinator.md for details
# ==============================================================================

# Disable authentication (development only, default: false)
# AUTH_DISABLED=true

# DEPRECATED: Use Auth0 OIDC instead. Will be removed in future version.
# API key for Agent Coordinator (required unless AUTH_DISABLED=true)
# Generate a secure key: openssl rand -hex 32
ADMIN_API_KEY=

# DEPRECATED: Use Auth0 OIDC instead. Will be removed in future version.
# API key for clients (Agent Runner, Dashboard, CLI commands)
# Should match ADMIN_API_KEY when using single admin key approach
AGENT_ORCHESTRATOR_API_KEY=

# ------------------------------------------------------------------------------
# Auth0 OIDC (optional)
# See docs/setup/auth0-setup.md for setup guide
# ------------------------------------------------------------------------------

# Auth0 tenant domain (e.g., your-org.auth0.com)
# AUTH0_DOMAIN=

# API identifier from Auth0 (must match dashboard VITE_AUTH0_AUDIENCE)
# AUTH0_AUDIENCE=

# Agent Runner M2M credentials (for machine-to-machine auth)
# Create a Machine-to-Machine app in Auth0 and authorize it for your API
# AUTH0_RUNNER_CLIENT_ID=
# AUTH0_RUNNER_CLIENT_SECRET=

# Dashboard/Chat-UI SPA credentials (for user login)
# Create a Single Page Application in Auth0
# AUTH0_DASHBOARD_CLIENT_ID=

# ==============================================================================
# GENERAL CONFIGURATION
# ==============================================================================

# Custom agent blueprints directory (default: ./.agent-orchestrator/agents)
# AGENT_DIR=/path/to/your/agents

# Observability backend debug logging (default: false)
# DEBUG_LOGGING=true

# Agent Orchestrator API URL (default: http://127.0.0.1:8765)
# Used by ao-* commands and the dashboard to connect to the unified backend service
# AGENT_ORCHESTRATOR_API_URL=http://127.0.0.1:8765

# Context Store: Semantic Search
# Requires Ollama running locally with embedding model pulled.
# See servers/context-store/README.md for full setup instructions.
# SEMANTIC_SEARCH_ENABLED=true
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# Agent Orchestrator MCP Server (HTTP mode)
# Used when running the MCP server in HTTP mode (make start-ao-mcp)
# Since HTTP mode runs as a standalone server, the project directory must be configured here
# AGENT_ORCHESTRATOR_PROJECT_DIR=/path/to/your/project
# AGENT_ORCHESTRATOR_MCP_PORT=9500
# AGENT_ORCHESTRATOR_MCP_HOST=127.0.0.1

# Context Store MCP Server (HTTP mode)
# Used when running the MCP server in HTTP mode (make start-cs-mcp)
# CONTEXT_STORE_MCP_PORT=9501
# CONTEXT_STORE_MCP_HOST=127.0.0.1
