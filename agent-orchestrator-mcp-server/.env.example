# Agent Orchestrator MCP Server Configuration

# REQUIRED: Absolute path to the agent-orchestrator.sh script
AGENT_ORCHESTRATOR_SCRIPT_PATH=/absolute/path/to/agent-orchestrator.sh

# Agent Orchestrator Script Environment Variables
# These control where the agent-orchestrator.sh script stores data and runs

# REQUIRED (minimum): Project directory where the claude command runs
# If not set, defaults to the directory where the script is called from
AGENT_ORCHESTRATOR_PROJECT_DIR=/path/to/your/project

# OPTIONAL: Custom location for agent session storage
# If not set, defaults to AGENT_ORCHESTRATOR_PROJECT_DIR/.agent-orchestrator/sessions
# AGENT_ORCHESTRATOR_SESSIONS_DIR=/custom/path/to/sessions

# OPTIONAL: Custom location for agent definitions
# If not set, defaults to AGENT_ORCHESTRATOR_PROJECT_DIR/.agent-orchestrator/agents
# AGENT_ORCHESTRATOR_AGENTS_DIR=/custom/path/to/agents

# MCP Server Debug Logging
# Enable debug logging for the MCP server (writes to logs/mcp-server.log)
# Set to "true" to enable, omit or set to "false" to disable
# MCP_SERVER_DEBUG=true
