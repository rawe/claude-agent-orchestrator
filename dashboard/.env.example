# Backend URLs
# Agent Orchestrator API (sessions, events, agent blueprints, runs - unified service)
# SSE endpoint is automatically derived as {API_URL}/sse/sessions
VITE_AGENT_ORCHESTRATOR_API_URL=http://localhost:8765
VITE_DOCUMENT_SERVER_URL=http://localhost:8766

# Auth0 Configuration (optional - enables login UI)
# When not configured, dashboard works without auth (for local development)
# See docs/setup/auth0-setup.md for setup guide
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
